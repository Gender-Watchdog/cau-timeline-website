import re

with open("index.html", "r", encoding="utf-8") as f:
    text = f.read()

# I will just write a python script to help stub it out, wait, I can just use `create_file` and output the Japanese text directly.
